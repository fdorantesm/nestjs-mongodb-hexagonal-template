export const sesConfig = Object.freeze({
  region: 'us-east-1',
  accessKey: 'accessKey',
  secretAccessKey: 'secretAccessKey',
});

export const emailConfig = Object.freeze({
  from: 'john@example.com',
  bcc: ['jane@example.com'],
  subject: 'Test Subject',
  template: '<p>Test Template</p>',
});
